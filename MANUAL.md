# サイト更新マニュアル

このサイトは、**パソコンに何もインストールしなくても、ブラウザだけで更新できます。**
必要なのは GitHub のアカウント（無料）だけです。

サイト： <https://tkikukawa.github.io/nakano-hp-demo/>

---

## 1. GitHubアカウントを作る（初回だけ・5分）

1. <https://github.com/signup> を開く
2. **メールアドレス**を入力 → Continue
3. **パスワード**を決めて入力 → Continue
   （英数字を混ぜて15文字以上、または8文字以上で数字と小文字を含むこと）
4. **ユーザー名**を決める → Continue
   これが公開されます。本名でなくて構いません。例：`taro-yamada-lab`
5. 製品案内メールの受信可否（`y` または `n`）→ Continue
6. パズル形式の認証をクリア
7. メールに届いた**8桁のコード**を入力

これで完了です。料金の入力は一切ありません。プランを聞かれたら **Free** を選びます。

> アカウントを作ったら、**ユーザー名を管理者に伝えてください。** 招待を送ります。

---

## 2. 招待を受け取る（初回だけ）

管理者が招待すると、登録したメールアドレスに GitHub から通知が届きます。
**「Accept invitation」** を押してください。これで編集できるようになります。

---

## 3. お知らせを1件追加する

いちばんよく使う操作です。

1. <https://github.com/tkikukawa/nakano-hp-demo/blob/main/data/news.csv> を開く
2. 右上の**鉛筆マーク（Edit this file）**をクリック
3. 表の**いちばん下**に、1行足す

```
2026/9/10,受賞,〇〇さんが△△賞を受賞しました。
```

書き方は **`日付,分類,本文`** の3つをカンマで区切るだけです。

| 項目 | 書き方 | 例 |
|---|---|---|
| 日付 | `2026/9/10` でも `2026-09-10` でもよい | `2026/9/10` |
| 分類 | 論文・受賞・学会・新聞・視察・行事 など | `受賞` |
| 本文 | ひとつづきの文章。**カンマは使わない** | `〇〇さんが△△賞を受賞しました。` |

4. 右上の **Commit changes...** をクリック
5. 変更内容を一言書く（例：`お知らせを1件追加`）→ **Commit changes**

**どこに足しても構いません。** 日付を見て自動で新しい順に並びます。

### 反映を確認する

コミットから **1〜2分**でサイトが更新されます。
更新されたかどうかは、リポジトリの **Actions** タブで確認できます。
緑のチェックが付いたら完了です。

---

## 4. 論文・受賞を追加する

<https://github.com/tkikukawa/nakano-hp-demo/blob/main/data/publications.csv> を同じ手順で編集します。

```
2026,論文,タイトルをここに書く,掲載誌名（IF 0.0）,yes
```

**`年,種別,タイトル,掲載先,OA`** の5つです。

- **種別** … `論文` か `受賞`
- **OA** … オープンアクセスなら `yes`、そうでなければ空欄（カンマの後ろに何も書かない）

---

## 5. メンバーを追加する

<https://github.com/tkikukawa/nakano-hp-demo/blob/main/data/members.csv>

```
山田 太郎,教授,材料組織学,yamada,person-1.svg
```

**`氏名,役職,専門分野,メール,写真`** の5つです。

- **メール** … `@` より前だけ書きます（`yamada` と書くと `yamada(at)example.ac.jp` と表示されます）
- **写真** … ファイル名。無ければ空欄でよく、その場合は仮の枠が表示されます
- **専門分野にカンマを入れたい**ときは、その項目全体を `"` で囲みます → `"材料組織学、結晶塑性学"`

---

## 6. ページの文章を直す

研究内容・研究設備・アクセスの本文は、`pages/` の中にあります。

| 直したいページ | 開くファイル |
|---|---|
| 研究内容 | [pages/research.html](pages/research.html) |
| 研究設備 | [pages/facilities.html](pages/facilities.html) |
| 問い合わせ・アクセス | [pages/access.html](pages/access.html) |

使われているのは、ほぼこの3つだけです。

```html
<p>ここが1つの段落になります。</p>

<h2 class="h2"><b>見出し</b></h2>

<tr><th>左の見出し</th><td>右の内容</td></tr>
```

**`<` から `>` までの記号は消さないでください。** 消すと表示が崩れます。
日本語の部分だけを書き換えるのが安全です。

---

## 7. 色を変える

[assets/genkou.css](assets/genkou.css) のいちばん上に、色をまとめてあります。

```css
:root{
  --blue:#003399;      /* 基本の青 */
  --blue-d:#001F5C;    /* 濃い青 */
  --tint:#EBF6FF;      /* 淡い青 */
  ...
}
```

`#003399` の部分を別の色コードに変えると、**サイト全体の色が一度に変わります。**

---

## 困ったとき

**サイトが更新されない**
Actions タブを見てください。赤い×が付いていたら、直前の編集のどこかが壊れています。
多くはカンマの数が合っていないか、`"` の閉じ忘れです。

**間違えて壊してしまった**
リポジトリの **History**（履歴）から、いつでも前の状態に戻せます。
**壊しても必ず元に戻せる**ので、恐れずに触ってください。

**分からないとき**
リポジトリの **Issues** タブから質問を書けます。

---

## やってはいけないこと

- **非公開の情報を書き込まない。** このリポジトリは公開されていて、
  一度書いたものは削除しても**履歴に残り続けます。**
  未公開の研究データ、個人の連絡先、パスワードなどは絶対に入れないでください。
- **`docs/` の中は直接編集しない。** ここは自動で作られる場所なので、
  直しても次の更新で上書きされます。編集するのは `data/` と `pages/` だけです。
- 各ページに入っている `<meta name="robots" content="noindex,nofollow">` の行は消さないでください。
