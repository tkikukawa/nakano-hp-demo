# 研究室ホームページ 更新マニュアル（自分のPCで作業する場合）

このマニュアルだけで、**準備から更新までの全部**ができます。
Git を使ったことがなくても大丈夫です。上から順に進めてください。

対象サイト → <https://tkikukawa.github.io/nakano-hp-demo/>

いまは練習用のサンプルで、中身は架空のものです。**間違えても必ず元に戻せます。**

---

## 毎回の流れ（覚えるのはこれだけ）

準備が終わったあとは、この4つを繰り返すだけです。

```
1. git pull          最新を取り込む（作業前に必ず）
2. ファイルを編集     data\ か pages\ の中
3. python build.py   サイトを作り直す
4. git add / commit / push   送る
```

---

## 管理者の対応を待つ場面は3か所だけ

このマニュアルの中で、**あなただけでは進められず、管理者の操作が必要になる**のは次の3か所です。
それ以外はすべて自分で完結します。

| いつ | あなたがすること | 管理者がすること | 目安 |
|---|---|---|---|
| **① 最初の1回** | GitHubのユーザー名を伝える | リポジトリに招待する | 連絡してから数分〜1日 |
| **② 招待が届いたら** | Accept invitation を押す | — | すぐ |
| **③ トラブルのとき** | 症状を伝える | 復旧・設定の修正 | 都度 |

**①と②が済めば、以降は毎回の更新に承認は要りません。** 自分で送れば自動で反映されます。

### 与えられた権限によって、できることが変わります

管理者から **Write** と **Read** のどちらかの権限が与えられます。
どちらか分からないときは管理者に確認してください。

| 権限 | 自分のPCから送れるか | 反映のされ方 |
|---|---|---|
| **Write** | **送れます** | 送った1〜2分後に自動で反映 |
| **Read** | **送れません**（`git push` が拒否されます） | ブラウザ編集が自動で「提案」になり、管理者が承認すると反映 |

**このマニュアルの第2部・第3部は Write 権限が前提です。**
Read 権限の場合は、末尾の「付録｜ブラウザだけで更新する方法」を使ってください。
操作は同じで、最後に管理者の承認が1回入るだけです。

---

# 第1部　準備（初回だけ・30分）

## 手順1｜GitHubアカウントを作る

無料です。カード情報の入力はありません。

1. <https://github.com/signup> を開く
2. **メールアドレス**を入力 → Continue
3. **パスワード**を決める → Continue
   （8文字以上で数字と小文字を含む、または15文字以上）
4. **ユーザー名**を決める → Continue
   これは公開されます。本名でなくて構いません（例：`taro-yamada-lab`）
5. 案内メールを受け取るか（`y` / `n`）→ Continue
6. パズル形式の認証をクリア
7. メールに届いた**8桁のコード**を入力
8. プランを聞かれたら **Free** を選ぶ

できたら、**決めたユーザー名を管理者に伝えてください。**

## 手順2｜招待を受け取る

**ここで管理者の操作が必要です。** 手順1で決めたユーザー名を管理者に伝え、招待を待ってください。

管理者が招待すると、登録したメールアドレスにGitHubから通知が届きます。
**「Accept invitation」** を押してください。これで編集できるようになります。

> 招待が届く前に手順3以降を進めても構いません。ソフトの準備は先にできます。
> ただし**手順7（clone）には招待の承諾が必要**な場合があります。

> 届かないときは迷惑メールフォルダを確認してください。
> それでも無ければ <https://github.com/tkikukawa/nakano-hp-demo/invitations> を直接開いてください。
> **招待は7日で期限切れ**になります。切れていたら管理者に再送を頼んでください。

## 手順3｜Git を入れる

ファイルをやり取りするための道具です。

1. <https://git-scm.com/download/win> を開く（Macの方は <https://git-scm.com/download/mac>）
2. ダウンロードされたファイルを実行
3. 選択肢が何度も出ますが、**すべて初期設定のまま「Next」** で構いません
4. 最後に「Install」→「Finish」

## 手順4｜Python を入れる

ページを生成するための道具です。

1. <https://www.python.org/downloads/> を開く
2. 黄色い「Download Python 3.x」ボタンを押す
3. ダウンロードされたファイルを実行
4. **最初の画面でいちばん下の「Add python.exe to PATH」に必ずチェックを入れる**（重要）
5. 「Install Now」を押す

## 手順5｜ちゃんと入ったか確かめる

**コマンドプロンプトの開き方**：キーボードで `Windows キー` を押し、`cmd` と入力して Enter。
（Macの方は「ターミナル」を開いてください）

黒い画面が出たら、次を打って Enter。

```bash
git --version
```

`git version 2.xx.x` のように表示されればOKです。続けて、

```bash
python --version
```

`Python 3.xx.x` と表示されればOKです。

> どちらかで「認識されていません」と出たら、**PCを再起動**してからもう一度試してください。
> それでも駄目なら、そのソフトの入れ直しが必要です。管理者に相談してください。

---

# 第2部　リポジトリを自分のPCに持ってくる（初回だけ）

## 手順6｜置き場所を決めて移動する

**置き場所はどこでも構いません**が、他のフォルダと混ざらない場所にしてください。
おすすめは**自分のユーザーフォルダの直下**です。

コマンドプロンプトで次を打ちます（`<あなたのユーザー名>` は自分のものに置き換えてください）。

```bash
cd C:\Users\<あなたのユーザー名>
```

Macの方はこちらです。

```bash
cd ~
```

## 手順7｜リポジトリを取ってくる（clone）

```bash
git clone https://github.com/tkikukawa/nakano-hp-demo.git
```

これで `nakano-hp-demo` というフォルダができ、中にファイル一式が入ります。
**この操作は最初の1回だけ**で、2回目以降は必要ありません。

### 初回はGitHubへのログインを求められます

`git clone` または最初の `git push` のとき、ブラウザが開いて
**「Sign in with your browser」** のような画面が出ます。
GitHubのアカウントでログインして許可すれば、以後は聞かれません。

> 画面が出ずに「username / password」を聞かれた場合は、**パスワードを入力しても通りません**。
> 管理者に連絡してください（設定を直します）。

## 手順8｜作業フォルダに入る

```bash
cd nakano-hp-demo
```

以降の作業は、すべてこのフォルダの中で行います。

> **次回からの入り方**：エクスプローラーで `nakano-hp-demo` フォルダを開き、
> 上のアドレスバーに `cmd` と打って Enter。そのフォルダでコマンドプロンプトが開きます。

---

# 第3部　更新する（毎回の作業）

## 手順9｜最新を取り込む

**編集を始める前に、必ずこれを実行してください。**

```bash
git pull
```

他の人が更新しているかもしれないためです。**これを忘れて編集すると、最後の送信でエラーになります。**

## 手順10｜ファイルを編集する

`nakano-hp-demo` フォルダの中の、次のファイルだけを触ります。

| 更新したいもの | 開くファイル |
|---|---|
| **お知らせ** | `data\news.csv` |
| **論文・受賞** | `data\publications.csv` |
| **メンバー** | `data\members.csv` |
| 研究内容の文章 | `pages\research.html` |
| 研究設備の文章 | `pages\facilities.html` |
| アクセスの文章 | `pages\access.html` |

CSVはダブルクリックすればExcelで開きます。HTMLはメモ帳で開いてください。

### お知らせを1件追加する

`data\news.csv` の**いちばん下**に1行足します。

```
2026/9/10,受賞,〇〇さんが△△賞を受賞しました。
```

**`日付,分類,本文`** の3つです。日付は `2026/9/10` でも `2026-09-10` でも構いません。
**どこに足しても、日付を見て自動で新しい順に並びます。**

### 論文・受賞を追加する

`data\publications.csv` に1行足します。

```
2026,論文,タイトルをここに書く,掲載誌名（IF 0.0）,yes
```

**`年,種別,タイトル,掲載先,OA`** の5つです。
種別は `論文` か `受賞`。OAはオープンアクセスなら `yes`、違えば空欄。

### メンバーを追加する

`data\members.csv` に1行足します。

```
山田 太郎,教授,材料組織学,yamada,person-1.svg
```

**`氏名,役職,専門分野,メール,写真`** の5つです。
メールは `@` より前だけ。写真が無ければ空欄で構いません。

### ページの文章を直す

`pages\` の中のファイルを開き、**日本語の部分だけ**を書き換えます。

```html
<p>ここが1つの段落になります。</p>
```

**`<` から `>` までの記号は消さないでください。** 消すと表示が崩れます。

### 共通の注意

**本文にカンマ（`,`）を使わないでください。** 項目の区切りと区別がつかなくなります。
読点は `、` を使えば問題ありません。
どうしても必要なときは、その項目全体を `"` で囲みます → `"材料組織学、結晶塑性学"`

## 手順11｜サイトを作り直す

```bash
python build.py
```

`完了。docs/ に 20 ファイル生成しました。` と出れば成功です。

**この操作を忘れると、データだけ変わってページが変わりません。**

### 手元で確認する

`docs\index.html` をダブルクリックすると、ブラウザで完成形を確認できます。
送る前にここで見ておくと安心です。

## 手順12｜変更をまとめる

```bash
git add -A
```

「変更したファイルを全部、送る箱に入れる」という意味です。

## 手順13｜記録する

```bash
git commit -m "お知らせを1件追加"
```

`-m` の後ろは何をしたかのメモです。日本語で構いません。あとから履歴として残ります。

## 手順14｜送る

```bash
git push
```

これでGitHubに送られます。

## 手順15｜反映を確認する

送ってから**1〜2分**でサイトが更新されます。

<https://github.com/tkikukawa/nakano-hp-demo/actions> を開き、
いちばん上に**緑のチェック**が付いていれば完了です。

サイトを開いて確認してください → <https://tkikukawa.github.io/nakano-hp-demo/>

---

## まとめて実行する

慣れたら、手順11〜14はこの1行で済みます。

```bash
python build.py && git add -A && git commit -m "お知らせを1件追加" && git push
```

---

# 第4部　困ったとき

## 「Updates were rejected」と出て送れない

**`git pull` を忘れた**か、作業中に他の人が更新したときに出ます。次を実行してください。

```bash
git pull --rebase
```

そのあともう一度、

```bash
git push
```

## 「CONFLICT」と出た

自分と他の人が**同じ行**を直したときに出ます。無理に進めず、次を実行して元に戻してください。

```bash
git rebase --abort
```

そのうえで管理者に連絡してください。

## サイトが更新されない

<https://github.com/tkikukawa/nakano-hp-demo/actions> で**赤い×**が付いていたら、
直前の編集のどこかが壊れています。ほとんどは次のどちらかです。

- **カンマの数が合っていない**（項目を1つ書き忘れた／余分に書いた）
- **`"` の閉じ忘れ**

CSVを見直して直し、もう一度 手順11〜14 を実行してください。

## 間違えて壊してしまった

**まだ送っていない場合** — 次を実行すると、編集前の状態に完全に戻ります。

```bash
git checkout -- .
```

**すでに送ってしまった場合** — 履歴から復元できます。管理者に連絡してください。
**壊しても必ず元に戻せる**ので、慌てなくて大丈夫です。

## いま何が変わっているか確認したい

```bash
git status
```

送る予定のファイルが一覧で出ます。

---

# やってはいけないこと

- **非公開の情報を書き込まない。** このリポジトリの中身は、一度書いたものを削除しても**履歴に残り続けます。** 未公開の研究データ、個人の連絡先、パスワードは絶対に入れないでください
- **`docs\` フォルダの中は直接編集しない。** `python build.py` が自動で作る場所なので、直しても次の更新で上書きされます。編集するのは `data\` と `pages\` だけです
- 各ページにある `<meta name="robots" content="noindex,nofollow">` の行は消さないでください（検索結果に出て本物のサイトと混ざるのを防いでいます）

---

# 付録｜ブラウザだけで更新する方法

**Git も Python も入れずに**、GitHubの画面だけで更新することもできます。
1〜2件の修正ならこちらの方が速いです。

1. <https://github.com/tkikukawa/nakano-hp-demo/blob/main/data/news.csv> を開く
2. 右上の**鉛筆マーク**をクリック
3. 編集する
4. 右上の **Commit changes...** → もう一度 **Commit changes**

**`python build.py` も `git push` も不要です。** 1〜2分後に自動でサイトへ反映されます。

書き方のルールは手順10と同じです。

詳しくは [MANUAL.md](MANUAL.md) を参照してください。
---

# 管理者向け｜あなた（管理者）が対応すること

担当者から連絡が来たときに行う操作です。担当者に渡すときは、この章は読み飛ばしてもらって構いません。

## ① 招待する（担当者1人につき1回・30秒）

1. <https://github.com/tkikukawa/nakano-hp-demo/settings/access> を開く
2. **Add people** をクリック
3. 担当者のGitHubユーザー名を入力し、候補から選ぶ
4. 権限を選ぶ（**Write** か **Read**）→ **Add ○○ to this repository**
5. 一覧に **Pending Invite（招待中）** と表示される
6. 担当者が Accept すると、表示が権限名に変わる

**自分のPCで作業してもらうなら Write が必要です。** Read ではローカルからの送信ができません。
初めての人には、まず Read ＋ ブラウザ編集で慣れてもらい、慣れたら Write に上げる進め方が安全です。

**招待は7日で期限切れ**になります。切れていたら同じ手順で再送してください。

## ② 提案を承認する（Read権限の担当者がいる場合）

担当者が編集すると、<https://github.com/tkikukawa/nakano-hp-demo/pulls> に提案が届きます。

1. 提案を開く
2. **Files changed** タブで、変更内容を確認する
3. 問題なければ **Merge pull request** → **Confirm merge**

merge した1〜2分後にサイトへ反映されます。
直したいところがあれば、コメントを書いて差し戻せます。

## ③ 復旧する

**担当者が壊してしまったとき**は、履歴から戻せます。

- <https://github.com/tkikukawa/nakano-hp-demo/commits/main> を開く
- 壊れる前のコミットを選ぶ
- そのコミットの画面から、内容を確認して戻す

**「CONFLICT」で担当者が止まっているとき**は、こちらで整理してから再度 pull してもらってください。

**担当者が `username / password` を求められて止まっているとき**は、
Git Credential Manager が入っていない可能性があります。Git の入れ直しを案内してください。

## ④ 担当を外す

卒業・異動のときは、手順①と同じページで、名前の横の歯車から **Remove** してください。
それ以降、その人はリポジトリにアクセスできなくなります。
**過去にその人が行った変更は履歴に残ります**（消える心配はありません）。

